#ifndef CLIB_H
#define CLIB_H

#define MAX(a,b) ((a) > (b) ? a : b)
#define MIN(a,b) ((a) < (b) ? a : b)

#define BETWEEN(start, end, value) (((value) >= (start)) && ((value) <= (end)))

#endif /* !CLIB_H */
